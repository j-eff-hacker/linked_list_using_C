# linked_list_using_C
You built an array-based linked list memory scanner in C, bypassing structs to manage a virtual RAM pool via parallel data and pointer arrays. You mastered logical traversal (current = pointer[current]) and coded a perfect fast/slow pointer rule for low-level cycle detection.
